name: Docs

on:
  pull_request:
    paths: ['modules/**']

jobs:
  terraform-docs:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: terraform-docs/gh-actions@v1
        with:
          find-dir: modules
          fail-on-diff: true
